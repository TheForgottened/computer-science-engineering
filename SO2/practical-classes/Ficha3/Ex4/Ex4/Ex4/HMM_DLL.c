// SO2_F3_MyDynamic_DLL.c: Defines the exported functions for the DLL.#include "pch.h" // use stdafx.h in Visual Studio 2017 and earlier#include <windows.h>#include <stdio.h>#include "HMM_DLL.h"// variable factordouble factor = 0;// Function to applyFactordouble applyFactor(double v) {    // return v * factor;  // Original    return (v * factor) * 2; // Duplicar o valor para mostrar que está ser chamada a DLL criada}