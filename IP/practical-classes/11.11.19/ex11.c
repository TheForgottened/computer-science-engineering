#include <stdio.h>

void main() {

	int num = 0, i = 0;

	do {
		scanf_s("%i", &num);

		if (num != 0) {
			max(num);
		}

		i++;
	} while (num != 0);

	printf("\n");
	printf("Sequencia: %i\n", i);
	printf("Maximo apareceu %i vezes.", max(-99999999999));
}

int max(int n, int oldn, int ) {

	if (n = )
		x++;
	else if (n > m)
		x = 1;

	return x;

}